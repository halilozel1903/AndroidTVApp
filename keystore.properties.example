# Copy to keystore.properties (gitignored) or export the ANDROID_* environment variables.
# Debug builds stay unsigned by default; release signing applies only when all values are set.

storeFile=release.keystore
storePassword=
keyAlias=
keyPassword=
